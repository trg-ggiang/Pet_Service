PORT=5050
CLIENT_URL=http://localhost:5173

SUPABASE_URL=https://cqwgjvfrptbdkwfnwiqw.supabase.co
# Dev-only default: use the publishable (anon) key so the backend can start.
# If some server operations fail due to RLS, replace with a Supabase Service Role key.
SUPABASE_KEY=sb_publishable_GUP3N43woi9CGQnMVJqncA_p-WeWNrf

JWT_SECRET=

# Optional: only needed if you use Prisma directly against Postgres.
DATABASE_URL=
