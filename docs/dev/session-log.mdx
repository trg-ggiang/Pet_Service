---
title: "Session Log"
description: "Ongoing development session notes."
---

## 2026-05-27 - AI Docs And Codex Setup

### Goal

Set up project documentation and AI coding configuration for Codex, Mintlify, and Understand Anything.

### Work Completed

- Added root onboarding docs and AI agent rules.
- Added project-local Codex config and Codex-specific instructions.
- Added Mintlify pages for quickstart, architecture, database, API, frontend, backend, AI workflow, developer setup, Git workflow, and reusable prompts.
- Added root `.env.example` and scrubbed example env files so they do not contain project URLs, publishable keys, or secrets.
- Updated `.gitignore` for Codex auth/session files, coverage, and safe VS Code exceptions.

### Files Changed

- `README.md`
- `AGENTS.md`
- `.codex/config.toml`
- `.codex/AGENTS.md`
- `.env.example`
- `.gitignore`
- `backend/.env.example`
- `frontend/.env.local.example`
- `docs/docs.json`
- `docs/*.mdx`
- `docs/dev/*.mdx`

### Test

- `npm run docs:check`

### Result

Pending final validation in this session.

### Open Issues

- Team must confirm production-safe migration and seed commands.
- Team must fill real local env values outside Git.

### Next Work

- Add request/response examples to API docs once backend contracts stabilize.
- Keep architecture docs updated after Understand Anything analysis.

### Suggested Commit Message

```text
docs: setup codex and mintlify workflow
```

## 2026-05-27 - Frontend Structure Refactor

### Goal

Refactor the frontend source tree into clearer responsibility-based folders without changing existing behavior or UI.

### Work Completed

- Moved page and portal screens from `src/app/components` to `src/pages`.
- Moved UI primitives to `src/components/ui`.
- Moved Figma image fallback component to `src/components/figma`.
- Moved API clients to `src/services`.
- Moved shared types to `src/types`.
- Moved reusable helpers to `src/utils`.
- Moved responsive hook to `src/hooks`.
- Added placeholder folders for `src/layouts`, `src/constants`, and `src/contexts`.
- Updated imports after file moves.

### Files Changed

- `frontend/src/app/App.tsx`
- `frontend/src/pages/*`
- `frontend/src/components/*`
- `frontend/src/services/*`
- `frontend/src/types/*`
- `frontend/src/utils/*`
- `frontend/src/hooks/*`
- `docs/frontend.mdx`

### Test

- `npm --prefix frontend run build`
- `npm --prefix frontend run lint`

### Result

Both checks passed. Vite reported a large chunk warning after build, but no build failure.

### Open Issues

- `frontend/src/app/App.tsx` is still large and should be split into smaller admin layout/dashboard components in a later scoped refactor.

### Next Work

- Extract admin dashboard data/constants and layout components from `App.tsx`.
- Add code splitting for large portal/page chunks if bundle size becomes a performance issue.

### Suggested Commit Message

```text
refactor: reorganize frontend source structure
```

## 2026-05-27 - Customer Dashboard Layout Fix

### Goal

Adjust the customer home dashboard layout so the quick action cards and upcoming appointments section fit the requested visual structure.

### Work Completed

- Stacked the booking, service history, and clinic finder action cards beside the welcome card on desktop.
- Converted the booking action card to the same compact horizontal style as the other action cards.
- Increased the upcoming appointments container height so appointment cards stay inside the parent panel.
- Matched the bottom edge of upcoming appointments with the unread notification panel in the desktop grid.
- Moved the new appointment button onto the appointment status filter row.
- Restyled the pet and service filters as compact selection cards with clearer typography and icons.
- Restored pet profile image frames and avatars to their original compact presentation.
- Extended the home pet summary card and upcoming appointments panel so child appointment cards fit within the parent container.
- Replaced appointment pet/service native select menus with custom dropdown option lists while keeping the selector bars full width.
- Expanded the service history type filter bar to match the width of the history list cards.
- Expanded the appointment status filter bar so it fills the space before the new appointment button.

### Files Changed

- `frontend/src/pages/CustomerPortal.tsx`
- `docs/dev/session-log.mdx`

### Test

- `npm --prefix frontend run build`

### Result

Build passed. Vite still reports the existing large chunk warning.

### Open Issues

- Browser plugin verification was unavailable in this session, so final validation used build output and source layout review.

### Suggested Commit Message

```text
fix: adjust customer dashboard layout
```

## 2026-05-27 - Add Pet Modal Input Enhancements

### Goal

Improve the add-pet profile modal so birthday and avatar inputs support both manual entry and picker-based selection.

### Work Completed

- Replaced the pet profile modal's native select controls with custom React dropdown menus.
- Changed the birthday field to support manual typing plus calendar selection while preserving future-date validation.
- Added local image file selection alongside the existing avatar URL input by converting the chosen file to a preview/data URL string.
- Kept the existing create-pet payload shape unchanged.

### Files Changed

- `frontend/src/pages/CustomerPetProfilesModule.tsx`
- `docs/dev/session-log.mdx`

### Test

- `npm --prefix frontend run build`

### Result

Build passed. Vite still reports the existing large chunk warning.

### Suggested Commit Message

```text
feat: improve add pet modal inputs
```

### Follow-up

- Split birthday into separate year, month, and day inputs with per-field constraints.
- Swapped the upload button text for an icon-only control.

## 2026-05-27 - Pet Profile Edit And Required Fields

### Goal

Allow editing existing pet profiles and make the create flow require all fields.

### Work Completed

- Added a shared pet form modal that supports both create and edit modes.
- Added a customer pet update endpoint in the backend and wired the frontend edit action to it.
- Added an edit button to the pet detail modal so existing profiles can be edited from their detail view.
- Made the create flow require all fields, including the health notes, and documented the "Không có" convention for empty allergy, disease, and special-note fields.

### Files Changed

- `frontend/src/pages/CustomerPetProfilesModule.tsx`
- `frontend/src/services/customerPets.ts`
- `frontend/src/types/customerPets.ts`
- `backend/src/services/customerPetsService.js`
- `backend/src/routes/customerPetsRoutes.js`
- `docs/dev/session-log.mdx`

### Test

- `npm --prefix frontend run build`

### Result

Build passed. Vite still reports the existing large chunk warning.

### Suggested Commit Message

```text
feat: add pet profile edit flow
```
