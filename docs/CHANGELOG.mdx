---
title: Changelog
description: Project documentation and implementation change history.
---

# Changelog

## Unreleased

- Updated the customer portal dashboard, appointment filters, service-history filters, and pet profile presentation for improved layout consistency.
- Refactored frontend source folders into `app`, `pages`, `components`, `services`, `hooks`, `types`, `utils`, `constants`, `contexts`, and `layouts`.
- Moved shared frontend types and helpers out of service/UI folders.
- Added root `README.md`, `AGENTS.md`, `.codex/config.toml`, and `.codex/AGENTS.md` for AI-assisted development.
- Added Mintlify pages for quickstart, architecture, database, API reference, frontend, backend, AI workflow, developer setup, Git workflow, prompts, and session log.
- Added root `.env.example` and scrubbed backend/frontend env examples so they do not contain project URLs or keys.
- Updated `.gitignore` for Codex auth/session files, coverage output, and VS Code exceptions.
- Updated `docs:check` to run `mint validate --telemetry false`.
- Restored Mintlify documentation setup under `/docs`.
- Restored `docs/docs.json` navigation.
- Restored root documentation scripts.
- Added `agent.mdx` workflow instructions.
